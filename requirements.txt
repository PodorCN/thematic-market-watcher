# Pipeline runtime dependencies (used by both local runs and CI).
anthropic>=0.40.0
openai>=1.50.0
pyyaml>=6.0
jinja2>=3.1
yfinance>=0.2.40
requests>=2.31
trafilatura>=1.8
python-dotenv>=1.0

# tests
pytest>=8.0
